modLoader = "javafml"
loaderVersion = "${loader_version_range}"
license = "${mod_license}"

[[mods]]
modId = "${mod_id}"
version = "${mod_version}"
displayName = "${mod_name}"
authors = "${mod_authors}"
description = '''${mod_description}'''
# 26.2 split the old logoFile in two: a wide banner and a square icon.
# Ours is square pixel art, so it is the icon.
iconFile = "logo.png"
displayURL = "https://www.curseforge.com/minecraft/mc-mods/fallout-enemy-hud-and-mutations"
issueTrackerURL = "https://github.com/JPZamps/fallout-enemy-hud-and-mutations/issues"

[[dependencies.${mod_id}]]
modId = "neoforge"
type = "required"
versionRange = "${neo_version_range}"
ordering = "NONE"
side = "BOTH"

[[dependencies.${mod_id}]]
modId = "minecraft"
type = "required"
versionRange = "${minecraft_version_range}"
ordering = "NONE"
side = "BOTH"
